export const metadata = {
  title: "Settings | Account, Addresses & Preferences",
  description:
    "Manage your account profile, shipping addresses, language preferences and security settings.",
  robots: "noindex, nofollow",
};

export default function SettingsLayout({ children }) {
  return children;
}
